# Byte-compiled / cache
__pycache__/
*.py[cod]

# Virtual environment
venv/
.venv/

# OAuth token state (local HTTP mode)
.oauth-state/

# Provena/legacy token files (should not be used, but just in case)
provena.key
provena_tokens.enc
provena_tokens.json
*instances.json
provena_instances.json

.tokens.json

tokens.json

# dotenv / secrets (templates like .env.example stay tracked)
.env
.env.*
!.env.example
!.env.template

# Local MCP config may contain API keys
.cursor/mcp.json

# OS files
.DS_Store
Thumbs.db

# Build artifacts
build/
*.egg-info/
