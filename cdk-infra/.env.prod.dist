# Prod MCP stack — separate AWS account with mds.gbrrestoration.org hosted zone
# Copy: cp .env.prod.dist .env.prod

AWS_ACCOUNT_ID=984415158203
AWS_REGION=ap-southeast-2
AWS_HOSTED_ZONE_ID=Z04262043960IOATPKUHV

BASE_DOMAIN=mds.gbrrestoration.org
MCP_SUBDOMAIN=provena-mcp
STACK_NAME=provena-mcp-prod
PROVENA_INSTANCE=mds.gbrrestoration.org

# MCP_HTTP_PATH=/
# MCP_CPU=256
# MCP_MEMORY_MIB=1024
# MCP_CONTAINER_IMAGE=<prod-account>.dkr.ecr.ap-southeast-2.amazonaws.com/provena-mcp:latest

# Secrets Manager ARNs in the PROD account (create via scripts/setup_mcp_*_aws.sh prod)
PROVENA_OFFLINE_TOKEN_SECRET_ARN=
MCP_OAUTH_PASSWORD_SECRET_ARN=
# OPENAI_API_KEY_SECRET_ARN=
